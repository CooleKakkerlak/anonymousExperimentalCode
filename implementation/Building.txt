We use CMake as build system.
In order to build the release version, do:
cmake --preset release


In order to build tests along with a non optimized executable:
cmake --preset test

You can then run the tests with:
ctest --test-dir test
